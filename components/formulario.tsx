interface FormsProps{
    list:string[]

}
export  const Formulario: React.FC<FormsProps> = ()=>{

    return(
        <>
        {/* <form>
            {list.map((elemento:string)=>(
                <>
                <label>{elemento}</label>
                <input type="text"/>
                </>
            ))}
        </form> */}
        </>
    )
}